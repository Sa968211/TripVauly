{
  "name": "tripvault-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "server": "node server/index.js",
    "client": "npm run dev --prefix client"
  }
}
